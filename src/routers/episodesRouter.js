import express from 'express';

export const router = express.Router();

// býr til nýjan þátt í season, aðeins ef notandi er stjórnandi
// router.post('/', catchErrors(allAvailable));

// skilar upplýsingum um þátt
// router.get('/{episodeId}', catchErrors(allAvailable));

// eyðir þætti, aðeins ef notandi er stjórnandi
// router.delete('/{episodeId}', catchErrors(allAvailable));
